plugins {
  id 'java'
  id 'org.opentorah.docbook'
}

configurePaper(
  'org.opentorah.calendar.paper.Tables',
  true
)

dependencies {
  runtimeOnly project(':calendar')
}

// To ensure calendar library is built before it is used:
site.dependsOn(':calendar:assemble')
