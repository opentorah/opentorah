package org.opentorah.docbook

import org.opentorah.util.Files
import org.opentorah.xml.{Dom, Resolver, Xml}
import org.scalatest.flatspec.AnyFlatSpecLike
import org.scalatest.matchers.should.Matchers
import java.io.File
import java.net.URL

class DocBookTest extends AnyFlatSpecLike with Matchers {

  "DocBook" should "parse" in {
//    val url: URL = Files.file2url(new File("calendar-paper/src/main/docBook/calendar.xml"))
//    val resolver: Resolver = new Resolver(new File("calendar-paper/src/main/xml/catalog.xml"))
//
//    val model = Dom
//    val docBook: model.Element = model.loadFromUrl(url, resolver = Some(resolver))
//    println(DocBook.prettyPrinter.render(docBook))
  }
}
