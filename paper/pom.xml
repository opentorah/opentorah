
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.podval</groupId>
        <artifactId>calendar</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <groupId>org.podval.calendar</groupId>
    <artifactId>org.podval.calendar.paper</artifactId>

    <name>Rambam on Calendar Paper</name>
    <packaging>pom</packaging>

    <properties>
        <!-- Looks like docbkx-fop-support plugin, which generates the metrix files,
             by default looks for "${fontsDirectory}/fileName" pattern in the configuration of the docbkx-maven-plugin!
             If the variable is called differently, build fails.
             If it does not point to a directory with the font files, build fails (differently ;)).
        -->
        <fontsDirectory>/usr/share/fonts/google-croscore/</fontsDirectory>
        <fontMetricsDirectory>${basedir}/target/fonts/</fontMetricsDirectory>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.podval.calendar</groupId>
            <artifactId>org.podval.calendar.astronomical</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <mainClass>org.podval.calendar.astronomical.Tables</mainClass>
                    <arguments>
                        <argument>${basedir}/target/tables</argument>
                    </arguments>
                </configuration>            
            </plugin>

            <plugin>
                <groupId>com.agilejava.docbkx</groupId>
                <artifactId>docbkx-maven-plugin</artifactId>

                <configuration>
                    <xincludeSupported>true</xincludeSupported>
                    <generatedSourceDirectory>${basedir}/target/tables</generatedSourceDirectory>

                    <includes>paper.xml</includes>

                    <bodyFontFamily>DejaVuSans</bodyFontFamily>
                    <tocSectionDepth>4</tocSectionDepth>

                    <externalFOPConfiguration>src/main/resources/fop.xconf</externalFOPConfiguration>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
