
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.podval</groupId>
        <artifactId>calendar-parent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>calendar-paper</artifactId>
    <name>Rambam on Calendar Paper</name>
    <packaging>jar</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>com.agilejava.docbkx</groupId>
                <artifactId>docbkx-maven-plugin</artifactId>
                <version>2.0.13</version>

                <dependencies>
                    <dependency>
                        <groupId>net.sf.docbook</groupId>
                        <artifactId>docbook-xml</artifactId>
                        <version>5.0-all</version>
                        <classifier>resources</classifier>
                        <type>zip</type>
                        <scope>runtime</scope>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <goals>
                            <goal>generate-html</goal>
                            <goal>generate-pdf</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
                <configuration>
                    <includes>moon.xml</includes>
                    <!--
                    TODO: did not work out with non-italics characters...
                    <bodyFontFamily>LiberationSans</bodyFontFamily>
                    <fonts>
                        <font>
                            <name>LiberationSans</name>
                            <style>sans</style>
                            <weight>normal</weight>
                            <embedFile>${basedir}/src/fonts/LiberationSans-Regular.ttf</embedFile>
                            <metricsFile>${basedir}/target/fonts/LiberationSans-Regular-metrics.xml</metricsFile>
                        </font>
                    </fonts>
                    -->
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.agilejava.docbkx</groupId>
                <artifactId>docbkx-fop-support</artifactId>
                <version>2.0.13</version>
                <executions>
                    <execution>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <ansi>true</ansi>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
