plugins {
  id 'org.podval.docbook-gradle-plugin'
  id 'scala'
}

dependencies {
  compile "org.scala-lang:scala-library:$scalaVersion"

  compile project(':library')
}

docBook {
  document = "calendar"

  dataGeneratorClass = "org.podval.calendar.paper.Tables"

  outputFormats = ["html", "pdf", "epub2"]

  substitutions = [
    "project.version": project.version
  ]

// Remove trailing period after honorific
// parameters.common = [ "punct.honorific": "" ]

  parameters.pdf = [
    "title.font.family"    : "DejaVu Sans",
    "body.font.family"     : "DejaVu Sans",
    "sans.font.family"     : "DejaVu Sans",
    "monospace.font.family": "DejaVu Sans Mono",
    "symbol.font.family"   : "DejaVu Sans",
    // "footnote.font.size": "10"
    "body.font.master": "12",
//    "orderedlist.label.width": "2em", // Some space for the list labels (is it needed?)
    "part.autolabel"       : "0",
    "chapter.autolabel"    : "0"
  ]

  mathJax {
    isEnabled = true
  }
}
